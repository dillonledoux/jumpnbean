export class FeaturedItem {
    photo: string;
    link: string;
    name: string;

    constructor(photo:string, link:string, name:string){
      this.photo = photo;
      this.link = link;
      this.name = name;
    }
}